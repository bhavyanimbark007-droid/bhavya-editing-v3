/**
 * This project uses Tailwind CSS v4 (CSS-first configuration).
 * All theme tokens (--color-lime, --font-display, etc.) live in
 * src/app/globals.css inside the `@theme` block — an exact copy of the
 * Vite project's src/index.css. No JS config is required; this file is
 * kept only so editors that look for one don't complain.
 */
export default {
  content: ["./src/**/*.{ts,tsx}"],
};
